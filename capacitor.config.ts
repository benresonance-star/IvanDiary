import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "au.com.myjournal.ivansdiary",
  appName: "Ivan's Diary",
  webDir: "dist",
};

export default config;
